const assert = require("assert"); const fs = require("fs"); const path = require("path"); const { loadCore, root } = require("../helpers/load_core.js"); loadCore(); const { Detector } = globalThis.PWM; const text = fs.readFileSync(path.join(root, "tests/fixtures/detection/cloud_provider_negative.txt"), "utf8"); assert.deepStrictEqual(new Detector().scan(text), []); console.log("PASS cloud provider negative fixture");
