(function () { const root = typeof globalThis !== "undefined" ? globalThis : window; root.PWM = root.PWM || {}; const { push } = root.PWM.EnterpriseDetectors.shared; const { enterpriseTokenScore, hasEnterpriseContext } = root.PWM.CloudScoring; function scan(text, detector) { const findings = []; const input = String(text || ""); for (let match, re = /\b(?:rg[-_][a-z0-9][a-z0-9_-]{4,62}|rgrp-[a-z0-9][a-z0-9-]{4,62}|resource-group-[a-z0-9][a-z0-9-]{4,62})\b/gi; (match = re.exec(input)) !== null;) { const raw = match[0]; let score = 42 + enterpriseTokenScore(raw, { requirePrefix: true }); if (/^(?:rg[-_]|rgrp-|resource-group-)/i.test(raw)) score += 18; if (hasEnterpriseContext(input, match.index, match.index + raw.length)) score += 10; if (score >= 72) push(findings, detector, raw, match.index, match.index + raw.length, "AZURE_RG", score, ["enterprise", "azure-rg", "scored"]); } return findings; } root.PWM.EnterpriseDetectors.azureResourceGroups = { scan }; })();
