(function () { const root = typeof globalThis !== "undefined" ? globalThis : window; root.PWM = root.PWM || {}; const { CLOUD_RESOURCE_PREFIXES } = root.PWM.DetectionConstants; const { push } = root.PWM.EnterpriseDetectors.shared; const { enterpriseTokens, enterpriseTokenScore, hasEnterpriseContext } = root.PWM.CloudScoring; function scan(text, detector) { const findings = []; const input = String(text || ""); for (let match, re = /\b(?:vnet|snet|pep|pl|rt|peer|sss|share|kv|nic|nsg|logic|aa|vm|aks|appgw|law|sql|db|dns|bastion|st)-[a-z0-9][a-z0-9-]{4,62}\b/gi; (match = re.exec(input)) !== null;) { const raw = match[0]; const tokens = enterpriseTokens(raw); if (!CLOUD_RESOURCE_PREFIXES.has(tokens[0])) continue; let score = 44 + enterpriseTokenScore(raw); if (hasEnterpriseContext(input, match.index, match.index + raw.length)) score += 8; if (score >= 70) push(findings, detector, raw, match.index, match.index + raw.length, "CLOUD_RESOURCE", score, ["enterprise", "cloud-resource", "scored"]); } return findings; } root.PWM.EnterpriseDetectors.cloudResourceNames = { scan }; })();
