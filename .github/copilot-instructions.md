When reviewing code, focus on:
- secret leakage
- redaction bypasses
- Unicode and zero-width bypasses
- browser extension security
- no raw secrets in logs, storage, or network payloads
- test coverage for adversarial inputs

Request changes if sensitive data can bypass redaction.
